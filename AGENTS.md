## Agent skills

### Issue tracker

任务与规格使用 GitHub Issues 管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的五类任务分流标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context：领域词汇位于根目录 `CONTEXT.md`，架构决策位于 `docs/adr/`。详见 `docs/agents/domain.md`。
