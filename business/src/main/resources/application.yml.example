# Environment variable names are defined by the repository-root .env.example.
rag:
  server:
    url: ${RAG_URL}
    slug: ${RAG_WORKSPACE_SLUG}
    auth:
      apikey: ${RAG_API_KEY}

amap:
  key: ${AMAP_API_KEY}
