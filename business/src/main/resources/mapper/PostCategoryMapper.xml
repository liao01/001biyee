<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiawa.lyw.mapper.PostCategoryMapper">
    <resultMap id="PostCategoryResultMap" type="com.jiawa.lyw.domain.PostCategory">
        <id column="code" property="code" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="sort_order" property="sortOrder" jdbcType="INTEGER" />
        <result column="enabled" property="enabled" jdbcType="BOOLEAN" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    </resultMap>

    <select id="selectEnabled" resultMap="PostCategoryResultMap">
        SELECT code, name, sort_order, enabled, create_time, update_time
        FROM post_category
        WHERE enabled = 1
        ORDER BY sort_order, code
    </select>

    <select id="selectByCode" resultMap="PostCategoryResultMap">
        SELECT code, name, sort_order, enabled, create_time, update_time
        FROM post_category
        WHERE code = #{code}
    </select>
</mapper>
