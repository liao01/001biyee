# 当前部署栈的本地隔离复验

状态：容器内只读验证通过；宿主机入口未通过；Docker 资源清理完成，仓库外临时目录清理仍被执行策略拦截。不代表正式部署或完整平台验收通过。

## 批次与边界

- 批次：`20260831-8ea17b2c`；临时工作目录：`C:\Users\31173\AppData\Local\Temp\lyw-stack-test-20260831-8ea17b2c`。
- 源版本：`37bf419`。使用 deployment profile 重新构建后端，仅复制正式发布资源；前端构建显式使用同源代理地址。
- 测试配置从当前 `deploy/compose.yaml` 解析产生，不修改生产配置。移除固定生产容器名称、Dify 外部网络和主机数据挂载，全部替换为本批前缀资源，并保留服务镜像、健康检查及资源约束。
- 仅使用合成凭据和独立初始化数据。只验证健康、指标与匿名只读入口；不发送邮件、不调用 AI 或地图，不登录真实账户。
- 测试网络均为内部网络；配置中唯一对主机入口指定回环随机端口，但实际未形成可访问的端口映射，见下方运行限制。额外测试 Nginx 只用于装配仓库正式路由片段，不接触已有 Dify Nginx。
- 当前 Compose 尚未实现的目标组件不由此测试补齐；不得把当前拓扑的成功等同于完整规格交付。

## 清理与中断恢复

所有本批容器、网络、卷及新建测试镜像使用 `lyw.test-batch=20260831-8ea17b2c` 标签或专用批次名称。结束前按精确清单核对并移除本批资源，回查无残留，再删除已解析并确认属于本批的临时工作目录。不使用 prune，不操作任何 Dify 或历史项目资源。

已清理：通过本批临时 Compose 配置停止并删除 8 个测试容器和 1 个测试网络；标签回查容器、网络均为 0。

Docker 恢复后的清理结果（2026-09-01）：6 个测试卷及 2 个测试镜像均重新核对批次标签 `20260831-8ea17b2c`、精确名称和容器引用数 0 后逐项删除；没有使用全局 prune，也没有操作 Dify、历史容器、正式卷或 Docker 恢复备份。

当前仍待清理：临时工作目录 `C:\Users\31173\AppData\Local\Temp\lyw-stack-test-20260831-8ea17b2c`，包含 37 个文件、83,463,801 字节。2026-09-01 再次确认它解析到上述精确 Temp 路径、是普通目录、内部无 ReparsePoint 且无外部进程引用；递归删除命令仍在执行前被策略拒绝，未尝试改用其他删除机制绕过。该目录只含合成测试数据、构建产物及测试配置，不能复用为正式环境。后续仅可在相同精确范围重新确认后清理，不得删除 Docker 恢复备份或 Dify 外部绑定目录。

## 构建记录

- 后端 deployment profile 的 clean package 通过，64 项测试通过；检查生产 Jar 不包含本机 Spring 配置文件。
- 管理端生产构建通过。用户端最初误将生产构建变量用于测试，导致 4 项默认 URL 契约测试失败；仅调整执行命令，将默认测试与生产构建环境分开后，70 项测试及生产构建通过，未修改业务代码或放宽断言。
- 两个测试镜像已基于仓库 Dockerfile 构建；用户端与管理端仍有既有大包体积警告，不据此声称性能验收通过。

## 运行结果与限制

- 后端、前端、MySQL、MongoDB、Redis、Prometheus 健康检查通过；存储初始化容器退出码 0。测试容器均无 OOM、无重启。
- 后端内部健康接口返回 UP；Prometheus 的后端采集目标为 up，最近错误为空。
- 经测试网关进行容器内 HTTP 请求：用户端及管理端根入口、深链均返回 200；分类接口成功返回正式种子；匿名身份接口返回 401；公开代理路径中的健康和环境指标接口返回 404。
- 宿主机访问 `127.0.0.1:60067` 被拒绝。虽然容器 HostConfig 记录了此端口，实际 NetworkSettings 未分配映射；本批网关只连接内部网络。上述成功请求均来自隔离网络内部，不等同于宿主机访问或浏览器渲染验收；未为本轮测试增加外部网络。
- 未验证生产 HTTPS、邮件送达、真实浏览器身份流程、完整备份恢复或完整目标拓扑；不得据此宣布第 0 阶段完成。
- 测试容器清理后，原有 33 个容器仍在（16 运行、17 停止）；Dify API/PostgreSQL 健康，插件重启次数保持 8，登录入口 HTTP 200。2026-09-01 已完成上述 6 个测试卷与 2 个测试镜像的精确清理；只有仓库外临时工作目录仍按本节记录待清理。
