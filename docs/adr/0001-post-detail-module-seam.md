# 帖子详情以 postId 作为唯一入口

帖子详情采用一个跨页面复用的 deep module，外部 interface 只接收 `postId` 与打开状态；帖子内容、互动状态、浏览记录和错误处理由 module 的 implementation 负责。公共详情与登录者个人状态使用分离的读取 interface，所有写操作返回服务端确认后的正式结果。这样选择是为了消除不同列表页面拼装帖子字段形成的多套事实源，同时允许匿名阅读而不耦合登录者状态；代价是需要同步收敛现有后端响应契约和四个前端调用位置。
